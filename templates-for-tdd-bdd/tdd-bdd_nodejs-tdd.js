'__copyright__ = "Algomus, Inc. 20XX"'

beforeEach(() => {
	//prepare context of functionality to be tested within following describe/suites
})

afterEach(() => {
  //cleaning, releasing resources etc.
})

// describe creates a block that groups together several related tests
describe('Noun', () => {

  test('should do something', () => {
    // expected output
  });

  test('should do something else', () => {
    // expected output
  });
});